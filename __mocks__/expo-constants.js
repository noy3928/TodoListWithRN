export default {
  // 여기에 필요한 상수를 추가하세요.
  // 예를 들어, 만약 Constants?.expoConfig?.extra?.apiUrl 값이 필요하다면 다음과 같이 작성하면 됩니다.
  expoConfig: {
    extra: {
      apiUrl: "http://localhost:3000",
    },
  },
}
